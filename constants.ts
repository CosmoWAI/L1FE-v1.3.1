
import { Stat, Cadence, Verification, Visibility } from './types';

export const STAT_OPTIONS = Object.values(Stat);
export const CADENCE_OPTIONS = Object.values(Cadence);
export const VERIFICATION_OPTIONS = Object.values(Verification);
export const VISIBILITY_OPTIONS = Object.values(Visibility);
